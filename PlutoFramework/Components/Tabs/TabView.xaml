<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlutoFramework.Components.Tabs.TabView"
             HeightRequest="40"
             HorizontalOptions="Fill"
             x:Name="view">
    <AbsoluteLayout HeightRequest="40"
                    HorizontalOptions="Fill">
        <VerticalStackLayout AbsoluteLayout.LayoutBounds="0.5, 0.5, 1, 1"
                             AbsoluteLayout.LayoutFlags="All"
                     HorizontalOptions="Center">
            <Image x:Name="icon"
           HeightRequest="20"
           WidthRequest="20"
           VerticalOptions="Center"
           IsVisible="False"/>
            <Label 
                VerticalTextAlignment="Center"
                x:Name="titleLabel"
                IsVisible="False"
                Text=""
                VerticalOptions="Center" />
        </VerticalStackLayout>
        <Border AbsoluteLayout.LayoutBounds="0.5, 1, 1, 4"
                AbsoluteLayout.LayoutFlags="PositionProportional,WidthProportional"
                BackgroundColor="Transparent"
                x:Name="underline"/>

        <AbsoluteLayout.GestureRecognizers>
            <TapGestureRecognizer x:Name="tapGestureRecognizer" />
        </AbsoluteLayout.GestureRecognizers>
    </AbsoluteLayout>
    
</ContentView>
